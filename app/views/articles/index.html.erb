<h1>Welcome to Articles-World</h1>
  <h2><%= link_to "Create a new article", new_article_path(@article) %></h2>
  <% @articles.each do |article| %>
    <h3><%= link_to article.title, article_path(article.id) %></h3>
    <ul>
      <li><%= article.content %></li>
    </ul>
  <% end %>
